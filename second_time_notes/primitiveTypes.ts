let myName: string = "this is my name";

let myAge: number = 100;

let hasFace: boolean = true;

let bodyParts: string[] = ["head", "shoulders", "knees", "toes"];

let myAddress: [string, number] = ["superstreet", 99];

enum Color {
    Silly,
    Billy,
    Boyo
}

function getMyName(): string {
    return myName;
}

function addNumbers(x: number, y: number): void {
    let addedNumber = x + y;
}

